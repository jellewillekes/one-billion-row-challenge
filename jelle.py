def main(measurements: str):
    # import args, which are the measurements.txt file
    import argparse

    parser = argparse.ArgumentParser(description="Process measurements froma file")
    parser.add_argument("")
